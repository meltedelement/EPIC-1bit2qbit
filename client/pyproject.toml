[project]
name = "epic-api"
version = "0.1.0"
description = "1bit2qbit client cryptographic funcitons"
requires-python = ">=3.11"
dependencies = [
    "doubleratchet>=1.0.0",
    "cryptography>=43",
    "argon2-cffi>=23.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
]
